
## 功能

✔ 设备列表 + 多选
✔ adb内置 + devices读取
✔ 单控 + 群控

✔ 分组管理
✔ WiFi连接
✔ 自动重连

✔ 屏幕预览（scrcpy）
✔ 主从同步控制
✔ 自动化脚本系统



## 一、 整体 UI 框架（最终形态）

┌────────────────────────────────────────────┐
│ 顶部栏（状态 + 全局操作）                  │
├──────────────┬─────────────────────────────┤
│ 左侧设备区   │ 中间预览区                  │
│              │                             │
│              │                             │
│              │                             │
├──────────────┴──────────────┬──────────────┤
│ 底部日志区                  │ 右侧控制面板 │
└─────────────────────────────┴──────────────┘

## 二、模块拆解（对应你的功能）

1. 顶部栏（全局控制）

[ADB状态] 🟢 running
[设备数量] 12 在线 / 3 离线

[按钮]
+ 添加设备（WiFi）
+ 扫描局域网
+ 重启ADB
+ 全部执行脚本
👉 关键点：

一眼看到系统是否正常
批量操作入口


2. 左侧：设备列表（核心）

每个设备卡片

☑ [设备名] Pixel_5（可编辑）
   SN: emulator-5554
   IP: 192.168.1.100
   分组：A组
   状态：🟢 在线
   连接：USB / WiFi

[按钮]
▶ 控制   🔁 重连   ❌ 移除


🔥 必做交互
✔ checkbox 多选
✔ shift 连选
✔ 分组筛选（dropdown）
✔ 搜索（设备名/IP）

3. 中间：屏幕预览区（scrcpy）
   
[模式切换]
● 单设备模式  
┌──────────────┐
│  手机实时画面 │
└──────────────┘
● 多设备网格模式（监控墙）
┌────┬────┬────┐
│ 1  │ 2  │ 3  │
├────┼────┼────┤
│ 4  │ 5  │ 6  │
└────┴────┴────┘

4. 右侧：控制面板（操作核心）

[控制] [群控] [脚本] [主从]


🧩 控制（单设备）

点击（tap）
滑动（swipe）
输入文字
启动App
返回/主页

🧩 群控（重点）

执行对象：
☑ 全部
☑ 分组
☑ 选中设备

操作：
- 点击某坐标
- 打开App
- 批量输入

[执行]

🧩 脚本系统（核心卖点🔥）

脚本列表：
- 自动聊天
- 自动点赞
- 自动回复

[选择脚本]
[执行范围：全部/分组/选中]
[开始执行]

🧩 主从控制（高级玩法）
主设备：Pixel_1
从设备：
☑ Pixel_2
☑ Pixel_3

模式：
● 实时同步
● 延迟执行

[开始同步]

5. 底部：日志区（非常关键）

[时间] [设备] [操作] [结果]

12:01 Pixel_1 tap(100,200) success
12:01 Pixel_2 tap(100,200) success
12:02 Pixel_3 error: device offline

👉 必须支持：
滚动
过滤
搜索

## 三 设备连接 UI

➕ 添加设备（WiFi）
弹窗：
IP地址： [192.168.1.xxx]
端口：   [5555]
[连接]

🔍 扫描设备
扫描范围[编辑]：
192.168.1.1 - 255
[开始扫描]


🔁 自动重连
UI提示：
    ⚠ Pixel_3 已断开
[自动重连中...]



## 四 核心组件结构 单页面设备中控 工程结构

 ├─ DeviceSidebar
 │   ├─ DeviceCard
 │   ├─ DeviceGroupFilter
 │
 ├─ PreviewPanel
 │   ├─ SingleView
 │   ├─ GridView
 │
 ├─ ControlPanel
 │   ├─ ControlTab
 │   ├─ BatchTab
 │   ├─ ScriptTab
 │   ├─ MasterSlaveTab


## 五、关键交互（必须做）
🔥 1. 设备选中 → 联动控制
选中设备 → 控制面板自动切换作用范围
🔥 2. 操作反馈
执行按钮 → loading → 成功/失败提示
🔥 3. 实时状态更新
设备掉线 → UI立即变红



## 新增模块：设备接入引导（Setup Wizard）

📍 顶部栏新增
[➕ 添加设备]  [🚀 快速接入向导]

二、接入流程 UI（分步骤）
Step 1：选择连接方式

请选择连接方式：

● USB连接（推荐）
● WiFi连接（无线调试）
● 模拟器（自动识别）

👉 这里要做成小白也能懂

Step 2：USB初始化（关键）
步骤提示：

1. 打开手机开发者模式
2. 打开 USB调试
3. 插入数据线
4. 点击【检测设备】

按钮：

[检测设备]

👉 成功后：

✔ 设备已识别：Pixel_5
✔ SN: xxxx
Step 3：一键开启 WiFi 调试（你提到的痛点🔥）

👉 这里你要做自动化：

方案1（推荐）

程序内部执行：

adb tcpip 5555
adb connect 192.168.x.x:5555

UI：

[一键开启无线连接]

👉 自动获取 IP：

adb shell ip route
⚠ Android 11+（重点）

WiFi调试变成：

pairing code
随机端口（你说的五位数）

👉 UI 必须支持：

IP: 192.168.1.10
端口: 37123
配对码: [输入]

[配对]

底层执行：

adb pair ip:port
adb connect ip:port

👉 这一步你不做，用户必炸

🧩 三、ATX / uiautomator2 自动安装（你漏的最关键点🔥）

👉 必须加检测：

检测设备环境...

❌ 未安装 ATX
❌ 未启动 uiautomator 服务

按钮：

[一键安装运行环境]
实现逻辑（核心）
adb push atx-agent /data/local/tmp
adb shell chmod +x /data/local/tmp/atx-agent
adb shell /data/local/tmp/atx-agent server -d

👉 UI 状态：

🟢 自动化环境：已就绪
🔴 自动化环境：未安装
🧩 四、权限检测（非常容易忽略）

必须自动检测：

USB调试授权
无障碍服务
悬浮窗权限（如果用）
屏幕录制权限（scrcpy）

UI：

⚠ 权限检测：

✔ USB调试：已开启
❌ 无障碍服务：未开启

按钮：

[打开设置]
🧩 五、最终完成页（非常重要）
🎉 设备接入完成！

✔ USB连接
✔ WiFi连接
✔ 自动化环境 OK

[进入控制台]
🧠 六、你原 UI 需要补的结构（架构级）

在你现有结构基础上新增：

├─ Onboarding
│   ├─ ConnectionStep
│   ├─ WifiSetupStep
│   ├─ AtxInstallStep
│   ├─ PermissionCheckStep
🔥 七、关键优化（直接决定产品成败）
1️⃣ 自动化优先原则

用户不应该手动输入：

IP（自动获取）
端口（自动识别）
命令（全部封装）
2️⃣ 异常兜底（必须）

比如：

❌ adb connect 失败

👉 提示：
- 手机和电脑不在同一WiFi
- 防火墙拦截