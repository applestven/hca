脚本目录 + manifest.json + 统一入口


## 一 工程结构（核心）
project/
├─ scripts/
│   ├─ auto_chat/
│   │   ├─ main.py
│   │   ├─ manifest.json
│   │   ├─ config.json
│   │   └─ assets/
│   │
│   ├─ auto_like/
│   │   ├─ main.py
│   │   ├─ manifest.json
│   │
│   └─ ...
│
├─ runner/
│   ├─ scriptRunner.ts   ← Electron调用入口
│


## 二 manifest.json（核心设计）

{
  "name": "自动聊天",
  "key": "auto_chat",
  "entry": "main.py",
  "version": "1.0.0",
  "description": "自动发送聊天消息",
  "author": "你",

  "params": [
    {
      "key": "message",
      "label": "发送内容",
      "type": "text",
      "default": "你好"
    },
    {
      "key": "delay",
      "label": "间隔时间",
      "type": "number",
      "default": 2
    }
  ]
}

## 🎯 三为什么要 manifest（关键）

你 UI 里可以直接生成👇

脚本：自动聊天

参数：
[输入框] 发送内容
[输入框] 间隔时间

[执行]

👉 不需要写死 UI，完全动态生成


## 🐍 四、Python 脚本规范（统一入口）

import sys
import json

def main():
    # 从 Electron 传入参数
    params = json.loads(sys.argv[1])
    
    message = params.get("message", "你好")
    delay = params.get("delay", 2)

    print(f"发送内容: {message}, 间隔: {delay}")

    # TODO: adb 操作

if __name__ == "__main__":
    main()

✔ Electron 调用方式    

import { spawn } from 'child_process'

function runScript(scriptPath, params) {
  const py = spawn('python', [
    scriptPath,
    JSON.stringify(params)
  ])

  py.stdout.on('data', (data) => {
    console.log(data.toString())
  })
}

## 🔗 五、脚本加载机制（自动发现）

✔ 扫描 scripts 目录

import fs from 'fs'
import path from 'path'

function loadScripts() {
  const base = path.join(process.resourcesPath, 'scripts')

  return fs.readdirSync(base).map(dir => {
    const manifestPath = path.join(base, dir, 'manifest.json')
    const manifest = JSON.parse(fs.readFileSync(manifestPath, 'utf-8'))

    return {
      ...manifest,
      path: path.join(base, dir)
    }
  })
}


## 🧩 六、UI 如何对接（关键）

脚本列表

点击后动态渲染参数

[输入框] message
[输入框] delay

[执行]

## 七、进阶设计（一步到位）

1. 支持设备参数注入
{
  "device": "emulator-5554",
  "message": "你好"
}

Python里：

device = params.get("device")

2. 支持多设备并发 
   
devices.map(d => runScript(script, { ...params, device: d }))

🔥 3. 脚本日志回传 UI
print(json.dumps({
  "type": "log",
  "msg": "执行成功"
}))

Electron解析：
const msg = JSON.parse(data)

🔥 4. 脚本分类（UI更好看）

{
  "category": "聊天"
}